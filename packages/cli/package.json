{
  "name": "@federlet/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "federlet": "./dist/index.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@federlet/tsconfig": "workspace:*"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.5.2",
    "commander": "^15.0.0"
  }
}
