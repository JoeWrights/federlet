/**
 * 全局环境变量 key。
 */
export const FEDERLET_GLOBAL_ENV_KEY = "__FEDERLET_ENV__";

/**
 * 默认运行时环境。
 */
export const DEFAULT_RUNTIME_ENV = "local";

/**
 * Shell 当前支持的 remote 挂载协议版本。
 */
export const SHELL_REMOTE_PROTOCOL_VERSION = "1";
