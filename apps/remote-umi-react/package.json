{
  "name": "@federlet/remote-umi-react",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "NODE_OPTIONS=--openssl-legacy-provider umi dev",
    "dev:rsbuild": "NODE_OPTIONS=--openssl-legacy-provider umi dev",
    "dev:vite": "NODE_OPTIONS=--openssl-legacy-provider umi dev",
    "dev:webpack": "NODE_OPTIONS=--openssl-legacy-provider umi dev",
    "build": "NODE_OPTIONS=--openssl-legacy-provider umi build",
    "build:rsbuild": "NODE_OPTIONS=--openssl-legacy-provider umi build",
    "build:vite": "NODE_OPTIONS=--openssl-legacy-provider umi build",
    "build:webpack": "NODE_OPTIONS=--openssl-legacy-provider umi build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist .umi .umi-production"
  },
  "dependencies": {
    "@federlet/shared-types": "workspace:*",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-router-dom": "5",
    "umi": "3.5.43"
  },
  "devDependencies": {
    "@federlet/tsconfig": "workspace:*",
    "@types/react": "17.0.83",
    "@types/react-dom": "17.0.26",
    "@types/react-router-dom": "5"
  }
}
